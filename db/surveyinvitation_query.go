// Code generated by ent, DO NOT EDIT.

package db

import (
	"context"
	"fmt"
	"math"
	"test/db/customer"
	"test/db/predicate"
	"test/db/project"
	"test/db/survey"
	"test/db/surveyinvitation"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
)

// SurveyInvitationQuery is the builder for querying SurveyInvitation entities.
type SurveyInvitationQuery struct {
	config
	ctx          *QueryContext
	order        []surveyinvitation.OrderOption
	inters       []Interceptor
	predicates   []predicate.SurveyInvitation
	withCustomer *CustomerQuery
	withProject  *ProjectQuery
	withSurvey   *SurveyQuery
	withFKs      bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SurveyInvitationQuery builder.
func (siq *SurveyInvitationQuery) Where(ps ...predicate.SurveyInvitation) *SurveyInvitationQuery {
	siq.predicates = append(siq.predicates, ps...)
	return siq
}

// Limit the number of records to be returned by this query.
func (siq *SurveyInvitationQuery) Limit(limit int) *SurveyInvitationQuery {
	siq.ctx.Limit = &limit
	return siq
}

// Offset to start from.
func (siq *SurveyInvitationQuery) Offset(offset int) *SurveyInvitationQuery {
	siq.ctx.Offset = &offset
	return siq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (siq *SurveyInvitationQuery) Unique(unique bool) *SurveyInvitationQuery {
	siq.ctx.Unique = &unique
	return siq
}

// Order specifies how the records should be ordered.
func (siq *SurveyInvitationQuery) Order(o ...surveyinvitation.OrderOption) *SurveyInvitationQuery {
	siq.order = append(siq.order, o...)
	return siq
}

// QueryCustomer chains the current query on the "customer" edge.
func (siq *SurveyInvitationQuery) QueryCustomer() *CustomerQuery {
	query := (&CustomerClient{config: siq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := siq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := siq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(surveyinvitation.Table, surveyinvitation.FieldID, selector),
			sqlgraph.To(customer.Table, customer.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, surveyinvitation.CustomerTable, surveyinvitation.CustomerColumn),
		)
		fromU = sqlgraph.SetNeighbors(siq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryProject chains the current query on the "project" edge.
func (siq *SurveyInvitationQuery) QueryProject() *ProjectQuery {
	query := (&ProjectClient{config: siq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := siq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := siq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(surveyinvitation.Table, surveyinvitation.FieldID, selector),
			sqlgraph.To(project.Table, project.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, surveyinvitation.ProjectTable, surveyinvitation.ProjectColumn),
		)
		fromU = sqlgraph.SetNeighbors(siq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QuerySurvey chains the current query on the "survey" edge.
func (siq *SurveyInvitationQuery) QuerySurvey() *SurveyQuery {
	query := (&SurveyClient{config: siq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := siq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := siq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(surveyinvitation.Table, surveyinvitation.FieldID, selector),
			sqlgraph.To(survey.Table, survey.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, surveyinvitation.SurveyTable, surveyinvitation.SurveyColumn),
		)
		fromU = sqlgraph.SetNeighbors(siq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first SurveyInvitation entity from the query.
// Returns a *NotFoundError when no SurveyInvitation was found.
func (siq *SurveyInvitationQuery) First(ctx context.Context) (*SurveyInvitation, error) {
	nodes, err := siq.Limit(1).All(setContextOp(ctx, siq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{surveyinvitation.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (siq *SurveyInvitationQuery) FirstX(ctx context.Context) *SurveyInvitation {
	node, err := siq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SurveyInvitation ID from the query.
// Returns a *NotFoundError when no SurveyInvitation ID was found.
func (siq *SurveyInvitationQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = siq.Limit(1).IDs(setContextOp(ctx, siq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{surveyinvitation.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (siq *SurveyInvitationQuery) FirstIDX(ctx context.Context) int {
	id, err := siq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SurveyInvitation entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SurveyInvitation entity is found.
// Returns a *NotFoundError when no SurveyInvitation entities are found.
func (siq *SurveyInvitationQuery) Only(ctx context.Context) (*SurveyInvitation, error) {
	nodes, err := siq.Limit(2).All(setContextOp(ctx, siq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{surveyinvitation.Label}
	default:
		return nil, &NotSingularError{surveyinvitation.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (siq *SurveyInvitationQuery) OnlyX(ctx context.Context) *SurveyInvitation {
	node, err := siq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SurveyInvitation ID in the query.
// Returns a *NotSingularError when more than one SurveyInvitation ID is found.
// Returns a *NotFoundError when no entities are found.
func (siq *SurveyInvitationQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = siq.Limit(2).IDs(setContextOp(ctx, siq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{surveyinvitation.Label}
	default:
		err = &NotSingularError{surveyinvitation.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (siq *SurveyInvitationQuery) OnlyIDX(ctx context.Context) int {
	id, err := siq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SurveyInvitations.
func (siq *SurveyInvitationQuery) All(ctx context.Context) ([]*SurveyInvitation, error) {
	ctx = setContextOp(ctx, siq.ctx, "All")
	if err := siq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*SurveyInvitation, *SurveyInvitationQuery]()
	return withInterceptors[[]*SurveyInvitation](ctx, siq, qr, siq.inters)
}

// AllX is like All, but panics if an error occurs.
func (siq *SurveyInvitationQuery) AllX(ctx context.Context) []*SurveyInvitation {
	nodes, err := siq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SurveyInvitation IDs.
func (siq *SurveyInvitationQuery) IDs(ctx context.Context) (ids []int, err error) {
	if siq.ctx.Unique == nil && siq.path != nil {
		siq.Unique(true)
	}
	ctx = setContextOp(ctx, siq.ctx, "IDs")
	if err = siq.Select(surveyinvitation.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (siq *SurveyInvitationQuery) IDsX(ctx context.Context) []int {
	ids, err := siq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (siq *SurveyInvitationQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, siq.ctx, "Count")
	if err := siq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, siq, querierCount[*SurveyInvitationQuery](), siq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (siq *SurveyInvitationQuery) CountX(ctx context.Context) int {
	count, err := siq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (siq *SurveyInvitationQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, siq.ctx, "Exist")
	switch _, err := siq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("db: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (siq *SurveyInvitationQuery) ExistX(ctx context.Context) bool {
	exist, err := siq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SurveyInvitationQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (siq *SurveyInvitationQuery) Clone() *SurveyInvitationQuery {
	if siq == nil {
		return nil
	}
	return &SurveyInvitationQuery{
		config:       siq.config,
		ctx:          siq.ctx.Clone(),
		order:        append([]surveyinvitation.OrderOption{}, siq.order...),
		inters:       append([]Interceptor{}, siq.inters...),
		predicates:   append([]predicate.SurveyInvitation{}, siq.predicates...),
		withCustomer: siq.withCustomer.Clone(),
		withProject:  siq.withProject.Clone(),
		withSurvey:   siq.withSurvey.Clone(),
		// clone intermediate query.
		sql:  siq.sql.Clone(),
		path: siq.path,
	}
}

// WithCustomer tells the query-builder to eager-load the nodes that are connected to
// the "customer" edge. The optional arguments are used to configure the query builder of the edge.
func (siq *SurveyInvitationQuery) WithCustomer(opts ...func(*CustomerQuery)) *SurveyInvitationQuery {
	query := (&CustomerClient{config: siq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	siq.withCustomer = query
	return siq
}

// WithProject tells the query-builder to eager-load the nodes that are connected to
// the "project" edge. The optional arguments are used to configure the query builder of the edge.
func (siq *SurveyInvitationQuery) WithProject(opts ...func(*ProjectQuery)) *SurveyInvitationQuery {
	query := (&ProjectClient{config: siq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	siq.withProject = query
	return siq
}

// WithSurvey tells the query-builder to eager-load the nodes that are connected to
// the "survey" edge. The optional arguments are used to configure the query builder of the edge.
func (siq *SurveyInvitationQuery) WithSurvey(opts ...func(*SurveyQuery)) *SurveyInvitationQuery {
	query := (&SurveyClient{config: siq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	siq.withSurvey = query
	return siq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Status surveyinvitation.Status `json:"status,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SurveyInvitation.Query().
//		GroupBy(surveyinvitation.FieldStatus).
//		Aggregate(db.Count()).
//		Scan(ctx, &v)
func (siq *SurveyInvitationQuery) GroupBy(field string, fields ...string) *SurveyInvitationGroupBy {
	siq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SurveyInvitationGroupBy{build: siq}
	grbuild.flds = &siq.ctx.Fields
	grbuild.label = surveyinvitation.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Status surveyinvitation.Status `json:"status,omitempty"`
//	}
//
//	client.SurveyInvitation.Query().
//		Select(surveyinvitation.FieldStatus).
//		Scan(ctx, &v)
func (siq *SurveyInvitationQuery) Select(fields ...string) *SurveyInvitationSelect {
	siq.ctx.Fields = append(siq.ctx.Fields, fields...)
	sbuild := &SurveyInvitationSelect{SurveyInvitationQuery: siq}
	sbuild.label = surveyinvitation.Label
	sbuild.flds, sbuild.scan = &siq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SurveyInvitationSelect configured with the given aggregations.
func (siq *SurveyInvitationQuery) Aggregate(fns ...AggregateFunc) *SurveyInvitationSelect {
	return siq.Select().Aggregate(fns...)
}

func (siq *SurveyInvitationQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range siq.inters {
		if inter == nil {
			return fmt.Errorf("db: uninitialized interceptor (forgotten import db/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, siq); err != nil {
				return err
			}
		}
	}
	for _, f := range siq.ctx.Fields {
		if !surveyinvitation.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("db: invalid field %q for query", f)}
		}
	}
	if siq.path != nil {
		prev, err := siq.path(ctx)
		if err != nil {
			return err
		}
		siq.sql = prev
	}
	return nil
}

func (siq *SurveyInvitationQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SurveyInvitation, error) {
	var (
		nodes       = []*SurveyInvitation{}
		withFKs     = siq.withFKs
		_spec       = siq.querySpec()
		loadedTypes = [3]bool{
			siq.withCustomer != nil,
			siq.withProject != nil,
			siq.withSurvey != nil,
		}
	)
	if siq.withCustomer != nil || siq.withProject != nil || siq.withSurvey != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, surveyinvitation.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*SurveyInvitation).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &SurveyInvitation{config: siq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, siq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := siq.withCustomer; query != nil {
		if err := siq.loadCustomer(ctx, query, nodes, nil,
			func(n *SurveyInvitation, e *Customer) { n.Edges.Customer = e }); err != nil {
			return nil, err
		}
	}
	if query := siq.withProject; query != nil {
		if err := siq.loadProject(ctx, query, nodes, nil,
			func(n *SurveyInvitation, e *Project) { n.Edges.Project = e }); err != nil {
			return nil, err
		}
	}
	if query := siq.withSurvey; query != nil {
		if err := siq.loadSurvey(ctx, query, nodes, nil,
			func(n *SurveyInvitation, e *Survey) { n.Edges.Survey = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (siq *SurveyInvitationQuery) loadCustomer(ctx context.Context, query *CustomerQuery, nodes []*SurveyInvitation, init func(*SurveyInvitation), assign func(*SurveyInvitation, *Customer)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*SurveyInvitation)
	for i := range nodes {
		if nodes[i].customer_survey_invitations == nil {
			continue
		}
		fk := *nodes[i].customer_survey_invitations
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(customer.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "customer_survey_invitations" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (siq *SurveyInvitationQuery) loadProject(ctx context.Context, query *ProjectQuery, nodes []*SurveyInvitation, init func(*SurveyInvitation), assign func(*SurveyInvitation, *Project)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*SurveyInvitation)
	for i := range nodes {
		if nodes[i].project_survey_invitations == nil {
			continue
		}
		fk := *nodes[i].project_survey_invitations
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(project.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "project_survey_invitations" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (siq *SurveyInvitationQuery) loadSurvey(ctx context.Context, query *SurveyQuery, nodes []*SurveyInvitation, init func(*SurveyInvitation), assign func(*SurveyInvitation, *Survey)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*SurveyInvitation)
	for i := range nodes {
		if nodes[i].survey_survey_invitations == nil {
			continue
		}
		fk := *nodes[i].survey_survey_invitations
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(survey.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "survey_survey_invitations" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (siq *SurveyInvitationQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := siq.querySpec()
	_spec.Node.Columns = siq.ctx.Fields
	if len(siq.ctx.Fields) > 0 {
		_spec.Unique = siq.ctx.Unique != nil && *siq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, siq.driver, _spec)
}

func (siq *SurveyInvitationQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(surveyinvitation.Table, surveyinvitation.Columns, sqlgraph.NewFieldSpec(surveyinvitation.FieldID, field.TypeInt))
	_spec.From = siq.sql
	if unique := siq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if siq.path != nil {
		_spec.Unique = true
	}
	if fields := siq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, surveyinvitation.FieldID)
		for i := range fields {
			if fields[i] != surveyinvitation.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := siq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := siq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := siq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := siq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (siq *SurveyInvitationQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(siq.driver.Dialect())
	t1 := builder.Table(surveyinvitation.Table)
	columns := siq.ctx.Fields
	if len(columns) == 0 {
		columns = surveyinvitation.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if siq.sql != nil {
		selector = siq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if siq.ctx.Unique != nil && *siq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range siq.predicates {
		p(selector)
	}
	for _, p := range siq.order {
		p(selector)
	}
	if offset := siq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := siq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// SurveyInvitationGroupBy is the group-by builder for SurveyInvitation entities.
type SurveyInvitationGroupBy struct {
	selector
	build *SurveyInvitationQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (sigb *SurveyInvitationGroupBy) Aggregate(fns ...AggregateFunc) *SurveyInvitationGroupBy {
	sigb.fns = append(sigb.fns, fns...)
	return sigb
}

// Scan applies the selector query and scans the result into the given value.
func (sigb *SurveyInvitationGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sigb.build.ctx, "GroupBy")
	if err := sigb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SurveyInvitationQuery, *SurveyInvitationGroupBy](ctx, sigb.build, sigb, sigb.build.inters, v)
}

func (sigb *SurveyInvitationGroupBy) sqlScan(ctx context.Context, root *SurveyInvitationQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(sigb.fns))
	for _, fn := range sigb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*sigb.flds)+len(sigb.fns))
		for _, f := range *sigb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*sigb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sigb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SurveyInvitationSelect is the builder for selecting fields of SurveyInvitation entities.
type SurveyInvitationSelect struct {
	*SurveyInvitationQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (sis *SurveyInvitationSelect) Aggregate(fns ...AggregateFunc) *SurveyInvitationSelect {
	sis.fns = append(sis.fns, fns...)
	return sis
}

// Scan applies the selector query and scans the result into the given value.
func (sis *SurveyInvitationSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sis.ctx, "Select")
	if err := sis.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SurveyInvitationQuery, *SurveyInvitationSelect](ctx, sis.SurveyInvitationQuery, sis, sis.inters, v)
}

func (sis *SurveyInvitationSelect) sqlScan(ctx context.Context, root *SurveyInvitationQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(sis.fns))
	for _, fn := range sis.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*sis.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sis.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Code generated by ent, DO NOT EDIT.

package db

import (
	"fmt"
	"strings"
	"test/db/survey"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
)

// Survey is the model entity for the Survey schema.
type Survey struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// Identifier holds the value of the "identifier" field.
	Identifier string `json:"identifier,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Description holds the value of the "description" field.
	Description string `json:"description,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SurveyQuery when eager-loading is set.
	Edges        SurveyEdges `json:"edges"`
	selectValues sql.SelectValues
}

// SurveyEdges holds the relations/edges for other nodes in the graph.
type SurveyEdges struct {
	// SurveyQuestionAnswers holds the value of the survey_question_answers edge.
	SurveyQuestionAnswers []*SurveyQuestionAnswers `json:"survey_question_answers,omitempty"`
	// SurveyInvitations holds the value of the survey_invitations edge.
	SurveyInvitations []*SurveyInvitation `json:"survey_invitations,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// SurveyQuestionAnswersOrErr returns the SurveyQuestionAnswers value or an error if the edge
// was not loaded in eager-loading.
func (e SurveyEdges) SurveyQuestionAnswersOrErr() ([]*SurveyQuestionAnswers, error) {
	if e.loadedTypes[0] {
		return e.SurveyQuestionAnswers, nil
	}
	return nil, &NotLoadedError{edge: "survey_question_answers"}
}

// SurveyInvitationsOrErr returns the SurveyInvitations value or an error if the edge
// was not loaded in eager-loading.
func (e SurveyEdges) SurveyInvitationsOrErr() ([]*SurveyInvitation, error) {
	if e.loadedTypes[1] {
		return e.SurveyInvitations, nil
	}
	return nil, &NotLoadedError{edge: "survey_invitations"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Survey) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case survey.FieldIdentifier, survey.FieldName, survey.FieldDescription:
			values[i] = new(sql.NullString)
		case survey.FieldCreatedAt, survey.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case survey.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Survey fields.
func (s *Survey) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case survey.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				s.ID = *value
			}
		case survey.FieldIdentifier:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field identifier", values[i])
			} else if value.Valid {
				s.Identifier = value.String
			}
		case survey.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				s.Name = value.String
			}
		case survey.FieldDescription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description", values[i])
			} else if value.Valid {
				s.Description = value.String
			}
		case survey.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				s.CreatedAt = value.Time
			}
		case survey.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				s.UpdatedAt = value.Time
			}
		default:
			s.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Survey.
// This includes values selected through modifiers, order, etc.
func (s *Survey) Value(name string) (ent.Value, error) {
	return s.selectValues.Get(name)
}

// QuerySurveyQuestionAnswers queries the "survey_question_answers" edge of the Survey entity.
func (s *Survey) QuerySurveyQuestionAnswers() *SurveyQuestionAnswersQuery {
	return NewSurveyClient(s.config).QuerySurveyQuestionAnswers(s)
}

// QuerySurveyInvitations queries the "survey_invitations" edge of the Survey entity.
func (s *Survey) QuerySurveyInvitations() *SurveyInvitationQuery {
	return NewSurveyClient(s.config).QuerySurveyInvitations(s)
}

// Update returns a builder for updating this Survey.
// Note that you need to call Survey.Unwrap() before calling this method if this Survey
// was returned from a transaction, and the transaction was committed or rolled back.
func (s *Survey) Update() *SurveyUpdateOne {
	return NewSurveyClient(s.config).UpdateOne(s)
}

// Unwrap unwraps the Survey entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (s *Survey) Unwrap() *Survey {
	_tx, ok := s.config.driver.(*txDriver)
	if !ok {
		panic("db: Survey is not a transactional entity")
	}
	s.config.driver = _tx.drv
	return s
}

// String implements the fmt.Stringer.
func (s *Survey) String() string {
	var builder strings.Builder
	builder.WriteString("Survey(")
	builder.WriteString(fmt.Sprintf("id=%v, ", s.ID))
	builder.WriteString("identifier=")
	builder.WriteString(s.Identifier)
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(s.Name)
	builder.WriteString(", ")
	builder.WriteString("description=")
	builder.WriteString(s.Description)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(s.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(s.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Surveys is a parsable slice of Survey.
type Surveys []*Survey
